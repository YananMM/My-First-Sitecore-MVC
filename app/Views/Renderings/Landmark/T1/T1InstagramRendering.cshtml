@using Landmark.Classes
@{
    var instagramSocial = new InstagramSocial();
    var images = instagramSocial.GetFromInstagram(SitecoreItems.LandmarkConfigItem.Fields["User Id"].Value, deep: true);
}
<section class="panel-area">
<section class="instagram-area">
    <div class="instagram-inner">
        <hgroup class="panel-text">
            <h4>@Html.Sitecore().Field("Find Us on Instagram Title")</h4>
            <h2>@Html.Sitecore().Field("Instagram Title")</h2>
        </hgroup>
        <ul class="clearfix">
            @foreach (var image in images)
            {
                <li>
                    <a href="" class="lazy-hidden" data-bg="@image.Url">
                        <div class="instagram-text hidden-xs  hidden-sm">
                            <img data-src="@image.ProfilePicture" class="instagram-user lazy-loaded">
                            <div class="instagram-info">
                                <h5>@image.User</h5>
                                @image.PublishTime
                            </div>
                        </div>
                    </a>
                </li>
            }
        </ul>
    </div>
</section>
</section>
