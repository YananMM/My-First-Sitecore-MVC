@using Landmark.Classes
@using Sitecore.Configuration
@using Sitecore.Data.Items
@model RenderingModel
@{
    Item contextItem = Sitecore.Context.Item;
    Item firstParentItem = contextItem.Parent;
    Item secondParentItem = firstParentItem.Parent;
}
<section class="page-title">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-md-8 col-xs-12">
                @if (firstParentItem.ID.ToString().Equals(ItemGuids.ShoppingItem))
                {
                    <h1>@Html.Sitecore().Field("Glossary Value", (Item) Factory.GetDatabase("web").GetItem(ItemGuids.AllBrandsGuidItem))<span>@Sitecore.Context.Item["Page Title"].ToUpper()</span></h1>
                }
                else
                {
                    <h1>@secondParentItem["Page Title"].ToUpper() | @firstParentItem["Page Title"].ToUpper()<span>@Sitecore.Context.Item["Page Title"].ToUpper()</span></h1>
                }

            </div>
            <div class="col-lg-4 col-md-4 col-xs-12 link-seeall"> </div>
        </div>
    </div>
</section>
